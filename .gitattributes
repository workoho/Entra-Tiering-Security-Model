# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# PowerShell files will always have CRLF line endings on checkout and in the repository.
*.ps1 text eol=crlf
*.psd1 text eol=crlf
*.psm1 text eol=crlf
*.ps1xml text eol=crlf
*.psc1 text eol=crlf
*.clixml text eol=crlf

# Windows script and batch files will always have CRLF line endings on checkout and in the repository.
*.cmd text eol=crlf
*.bat text eol=crlf

# Set svg to binary type, as SVG is unlikely to be editted by hand. Can be treated as checked in blob
*.svg binary

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.pdf binary
